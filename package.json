{
  "name": "chrono-pmtiles-card",
  "version": "1.0.0",
  "scripts": {
    "build": "terser src/chrono-pmtiles-card.js -o dist/chrono-pmtiles-card.js --compress --mangle"
  },
  "devDependencies": {
    "terser": "^5.0.0"
  }
}
